---
"pi-sub2api-provider": patch
---

Recognize GPT-6 models as reasoning-capable when upstream metadata is absent. Honor boolean reasoning metadata from the models endpoint while keeping explicit local configuration highest priority.
